import { Form } from 'antd';
import styled from 'styled-components';

export const StyledDevData = styled.main`
  width: 100%;
  height: 100vh;
  overflow: hidden;
  padding: 32px 48px;
`;

export const StyledPostStoryForm = styled(Form)``;
